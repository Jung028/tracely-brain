{
  "name": "tracely-brain",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "migrate": "bun run scripts/migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
